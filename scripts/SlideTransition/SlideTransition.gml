/// @description SlideTransition(mode, targetroom)
/// @arg Mode sets transition mode between next, restart, and got.
/// @arg Target sets target room when using the goto mode.

function SlideTransition(argument0, argument1)
{


	
}